import './navBar.css'

function NavBar() {
  return (
    <div class="navbar">
        <a class="centerAlign" href="#"> Home</a>
        <a class="centerAlign" href="#"> About</a>
        <a class="centerAlign" href="#"> Contact</a>
        <a class="login" href="#" > Login</a>
    </div>
  );
}

export default NavBar;
